Bootstrap: docker
From: python:3.8

%setup

%files
    monte_carlo_graph_search monte_carlo_graph_search

%post
    apt-get -y update
    ls
    pip install -r monte_carlo_graph_search/requirements.txt


%runscript
    echo "PLAYGROUND.DEF"
    echo $SEED
    echo "PLAYGROUND.DEF"
    bash scripts/launch_playground.sh $SEED
