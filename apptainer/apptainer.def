Bootstrap: docker
From: python:3.8
%setup

%files
    monte_carlo_graph_search monte_carlo_graph_search

%post
    apt-get -y update
    pip install -r monte_carlo_graph_search/requirements.txt

%runscript
    bash monte_carlo_graph_search/scripts/launch_exp.sh
